streamlit==1.32.0
python-dotenv
openai
transformers
torch
torchvision
# Vision
pillow            # for image loading
numpy
gdown
langgraph
accelerate        # improves loading & inference
sentence-transformers  # optional, for embeddings
# PDF & text extraction
pypdf             # lightweight PDF reader
unstructured      # better PDF parsing (optional, more accurate)
beautifulsoup4    # to extract hyperlinks from PDF text (if HTML-like)
lxml              # HTML parsing backend
faiss-cpu

# Utilities
tqdm              # progress bars

langchain==0.1.20
langchain-core==0.1.52
langchain-community==0.0.38
langchain-text-splitters
langchain-openai==0.1.6
